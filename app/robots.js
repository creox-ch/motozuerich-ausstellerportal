/**
 * Портал целиком закрыт от поисковиков — решение 2026-08-11.
 *
 * Это рабочий инструмент, а не витрина для поиска: поисковая витрина проекта
 * это motozuerich.ch. Ссылки на план залов и на вход рассылаются адресно.
 *
 * Запрет обхода дублируется метатегом noindex на страницах: robots.txt
 * запрещает читать страницу, но сам по себе не гарантирует, что адрес
 * не всплывёт в выдаче. Вместе — гарантирует.
 */
export default function robots() {
  return {
    rules: [{ userAgent: '*', disallow: '/' }],
  };
}
