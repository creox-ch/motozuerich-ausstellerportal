# Состояние портала экспонентов

Обновлено 2026-08-12. Правила работы — [`CLAUDE.md`](CLAUDE.md),
задачи — [`docs/backlog.md`](docs/backlog.md).

---

## 🔴 ГДЕ МЫ СЕЙЧАС

Портал живой и работает. Настоящих экспонентов в нём **нет** — только один
технический доступ. Продажа площадей ещё не открыта.

Остаток бэклога в основном ждёт данных от бизнеса — цен, сроков, квот.

## ✅ Момент выдачи доступа — решено 12.08

**В портале только те, кто уже согласован и оплатил.** Ссылка на вход уходит
после оплаты, а не сразу после согласования заявки — решение Иванны, отменяет
более раннее описание Ксении от 11.08.

```
план залов → заявка → согласование по телефону/почте
   → счёт за площадь ВНЕ портала → оплата
   → Messeleitung выдаёт доступ в админке → письмо-приглашение
   → кабинет
```

**Счета в портале при этом остаются.** Внутри кабинета экспонент заказывает
технику, рекламу, парковку — за эти заказы выставляются **дополнительные
счета**. То есть «Dokumente & Rechnungen» — не архив оплаченного, а рабочий
раздел: первый счёт (за площадь) приходит туда уже закрытым, следующие
выставляются внутри портала.

**Что при этом отложено — решение Иванны 12.08:** «как юзер попадает на портал —
пока оставляем на потом, нет ещё данных». Письмо-приглашение, онбординг и гейт
«почта за цену» **не делаем и не переспрашиваем**. Работаем над внутрянкой
кабинета; факт «в кабинете только оплатившие» служит для неё допущением.

## Что работает на проде

| | |
|---|---|
| Вход экспонента | почта → код из письма (Supabase Auth + Resend) |
| Кабинет | Übersicht с чек-листом сроков, Hallenplan, Firmenprofil, Technik & Service, Marketing, Anreise, Aktivitäten, Gemeinsame Aktivitäten, Dokumente & Rechnungen, Nachrichten |
| | + Ausweise & Tickets — предпросмотр без полей ввода |
| Витрина | публичный план залов, 43 площадки, заявка на площадь |
| Админка Messeleitung | заявки, компании, доступы, площадки, документы, переписка, активности, логистика, совместный маркетинг, маркетинг-заявки |
| Юридические страницы | Datenschutz и Impressum — **черновики**, в футере |

271 тест в CI + живой слой, который гоняется руками.

⚠️ **Ни один экран не проверен сессией живого человека.** Автотесты покрывают
логику и права; клика руками по готовому порталу ещё не было. План проверки
готов — [`docs/testplan-manuell.md`](docs/testplan-manuell.md), делает Иванна 13.08.

## Что ждёт людей

**Ксения** — полный список с объяснением, что каждый ответ открывает:
[`docs/fragen-an-messeleitung.md`](docs/fragen-an-messeleitung.md). Кратко:
модель цены площади, цены техники и рекламы, восемь сроков, квоты ворот
и параметры зала, контингенты пропусков, документы, реквизиты для Impressum,
план StageOne. Плюс решения: Vercel Pro и свой домен.

**Иванна:**
- ручная проверка портала по [`docs/testplan-manuell.md`](docs/testplan-manuell.md)
- передать Datenschutz и Impressum на вычитку юристу
  (`docs/rechtliches-review.md`) — нужно до того, как ссылку на витрину дадут
  наружу: форма уже собирает данные посторонних людей

## 🔧 Открытые хвосты на 12.08, вечер

- **`PORTAL_NOTIFY_EMAIL` не действует в Production.** Задана 12.08, но
  `/api/health` показывает пустой `aktiveUmleitungen` — скорее всего снята
  галочка Production. Пока так, уведомления о заявках и сообщениях идут
  `yves@motozuerich.ch`. Проверяется одним запросом к `/api/health`.
- **Свой домен `ausstellerportal.motozuerich.ch`** — решено делать, не начато.
  Pro для этого не нужен. Порядок: добавить домен в Vercel → внести показанную
  им запись в Hostpoint (доступ у Ива) → задать `PUBLIC_BASE_URL`. Сессии
  на новом домене начнутся заново — сейчас это безболезненно, потом нет.
- Ветка `feat/next-skeleton` смержена, но не удалена.

## Кабинет собран целиком — 12.08

**Все одиннадцать разделов прототипа есть в портале.** Сначала построено то, что
не требует выдуманных данных (план залов, документы и счета, переписка), затем —
по решению Иванны «где можно, пользуйся заглушками как в прототипе» — остальное:
чек-лист сроков, активности на стенде, Anreise, совместный маркетинг и остальные
блоки Marketing.

Два отступления от прототипа, оба намеренные:

- **Anreise — заявка, а не бронь.** Квоты ворот неизвестны, а бронь против
  выдуманной квоты даёт двух экспонентов у одних ворот в один час. Экспонент
  оставляет пожелание и данные машины, окно назначает Messeleitung.
- **Ausweise — предпросмотр без единого поля ввода.** Раздел собирает имена
  чужих сотрудников, форма появится только после вычитки Datenschutz. Запрет
  закреплён тестом, а не памятью — `tests/unit/ausweise-kein-eingabefeld.spec.js`.

Плашки о незавершённости — общий компонент `app/portal/noch-nicht.jsx`,
две фиксированные формулировки: «ввод не принимаем» и «в работе».

Вне кабинета не заблокировано: уведомление заявителю о статусе заявки,
ограничение частоты на публичной форме, мониторинг ошибок на проде.

## Что нельзя делать до вычитки Datenschutz

**Раздел Ausweise** (списки персонала). Это имена и функции сотрудников —
самая чувствительная категория данных в портале, и человек из списка нам
ничего не разрешал.

## Перед запуском

- убрать тестовые данные: `supabase/cleanup-testdaten.sql`
- снять плашки «Entwurf» с юридических страниц после вычитки
- перейти на ветку + PR вместо прямого пуша в `main`
- задать `PUBLIC_BASE_URL`, если появится свой домен
